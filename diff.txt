--- biloop_integration/public/index.html	2026-04-02 02:11:13
+++ index_fix_example_v3.html	2026-04-02 16:16:40
@@ -3,7 +3,7 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Panel de Integración Biloop</title>
+    <title>Boomerang — Invoicing Hub</title>
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
     <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,300..700;1,14..32,300..700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
@@ -514,16 +514,16 @@
             background-color: var(--yellow-bg); border-color: var(--yellow-border); color: var(--yellow);
         }
 
-        /* ── Date inputs — color-coded columns ────── */
-        .date-col-header    { color: var(--blue) !important; }
-        .est-date-col-header { color: var(--purple) !important; }
-        .pay-date-col-header { color: var(--green) !important; }
+        /* ── Date inputs — unified neutral style ───── */
+        .date-col-header,
+        .est-date-col-header,
+        .pay-date-col-header { }
 
         .col-hint {
             display: block;
             font-size: 9px;
             font-weight: 400;
-            opacity: 0.6;
+            color: var(--text-ghost);
             margin-top: 1px;
             text-transform: none;
             letter-spacing: 0.01em;
@@ -544,11 +544,11 @@
 
         .date-input:hover,
         .est-date-input:hover,
-        .pay-date-input:hover { border-color: var(--border-strong); }
+        .pay-date-input:hover { border-color: var(--border-strong); background: var(--bg-hover); }
 
-        .date-input:focus     { outline: none; border-color: var(--blue);   box-shadow: 0 0 0 2px rgba(59,130,246,0.12); }
-        .est-date-input:focus { outline: none; border-color: var(--purple); box-shadow: 0 0 0 2px rgba(139,92,246,0.12); }
-        .pay-date-input:focus { outline: none; border-color: var(--green);  box-shadow: 0 0 0 2px rgba(62,207,142,0.12); }
+        .date-input:focus,
+        .est-date-input:focus,
+        .pay-date-input:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 0 2px var(--accent-bg); }
 
         /* ── Stat cards ────────────────────────────── */
         .stats-bar {
@@ -608,11 +608,12 @@
         }
 
         .stat-card-value {
-            font-size: 28px;
+            font-size: 26px;
             font-weight: 650;
             font-family: 'JetBrains Mono', monospace;
             letter-spacing: -0.04em;
             line-height: 1;
+            color: var(--text-primary);
         }
 
         .stat-card.paid   .stat-card-value { color: var(--green); }
@@ -622,21 +623,30 @@
         .stat-card.overdue .stat-card-value { color: var(--red); }
         .stat-card.overdue .dot { background: var(--red); }
 
+        /* Active filter state */
+        .stat-card.filter-active {
+            border-color: var(--border-strong);
+            background: var(--bg-elevated);
+        }
+        .stat-card.filter-dimmed {
+            opacity: 0.3;
+        }
+
         /* ── Hint banners ──────────────────────────── */
         .workflow-hint {
             display: flex;
             align-items: center;
             gap: 10px;
-            background: var(--blue-bg);
-            border: 1px solid var(--blue-border);
+            background: var(--bg-surface);
+            border: 1px solid var(--border);
             border-radius: var(--r-md);
-            padding: 10px 14px;
+            padding: 9px 14px;
             margin-bottom: 16px;
             font-size: 12px;
-            color: var(--text-secondary);
+            color: var(--text-tertiary);
         }
 
-        .workflow-hint .hint-icon { color: var(--blue); flex-shrink: 0; display: flex; }
+        .workflow-hint .hint-icon { color: var(--text-ghost); flex-shrink: 0; display: flex; }
         .workflow-hint .hint-icon svg { width: 14px; height: 14px; stroke: currentColor; fill: none; stroke-width: 1.8; }
 
         .workflow-hint .steps {
@@ -657,8 +667,8 @@
             width: 16px;
             height: 16px;
             border-radius: 50%;
-            background: var(--blue);
-            color: var(--bg-base);
+            background: var(--text-ghost);
+            color: var(--text-primary);
             font-size: 9.5px;
             font-weight: 700;
             display: flex;
@@ -914,12 +924,14 @@
             padding: 16px;
             display: flex;
             flex-direction: column;
-            height: 58vh;
+            height: 60vh;
         }
 
         .chat-input-row {
             display: flex;
             gap: 8px;
+            padding-top: 12px;
+            border-top: 1px solid var(--border);
         }
         .chat-input-row input {
             flex: 1;
@@ -936,19 +948,88 @@
         .chat-input-row input:focus { border-color: var(--accent); box-shadow: 0 0 0 2px var(--accent-bg); }
         .chat-input-row input::placeholder { color: var(--text-ghost); }
 
-        /* ── Unsaved rows ──────────────────────────── */
-        tr.unsaved td { background-color: var(--red-bg) !important; }
+        /* AI welcome message */
+        .ai-welcome-msg {
+            display: flex;
+            gap: 10px;
+            align-items: flex-start;
+            max-width: 85%;
+        }
 
-        .pulse-red {
-            background: linear-gradient(180deg, #f87171 0%, #ef4444 100%) !important;
-            color: #fff !important;
+        .ai-avatar {
+            width: 28px;
+            height: 28px;
+            border-radius: var(--r-sm);
+            background: linear-gradient(135deg, var(--accent-bright) 0%, var(--accent-dim) 100%);
+            display: flex;
+            align-items: center;
+            justify-content: center;
+            flex-shrink: 0;
+            color: var(--bg-base);
+        }
+
+        /* Chat message bubbles */
+        .chat-msg {
+            padding: 10px 14px;
+            border-radius: var(--r-md);
+            max-width: 85%;
+            white-space: pre-wrap;
+            line-height: 1.6;
+            font-size: 13px;
+        }
+
+        .chat-msg.user {
+            background: var(--bg-overlay);
+            color: var(--text-primary);
+            align-self: flex-end;
+            border: 1px solid var(--border);
+        }
+
+        .chat-msg.model {
+            display: flex;
+            gap: 10px;
+            align-items: flex-start;
+            align-self: flex-start;
+            background: transparent;
+            color: var(--text-secondary);
+            padding: 0;
+        }
+
+        .chat-msg.model .msg-content {
+            padding: 10px 14px;
+            background: var(--bg-elevated);
+            border: 1px solid var(--border);
+            border-radius: var(--r-md);
+            flex: 1;
+        }
+
+        .chat-msg.error {
+            background: var(--red-bg);
+            color: var(--red);
+            align-self: center;
+            border: 1px solid var(--red-border);
+            font-size: 12px;
+        }
+
+        /* ── Unsaved rows — amber highlight ────────── */
+        tr.unsaved td {
+            background-color: rgba(234, 179, 8, 0.04) !important;
+            border-bottom-color: rgba(234, 179, 8, 0.1);
+        }
+        tr.unsaved td:first-child {
+            box-shadow: inset 3px 0 0 var(--yellow);
+        }
+
+        .pulse-red {
+            background: linear-gradient(180deg, var(--accent-bright) 0%, var(--accent) 100%) !important;
+            color: var(--bg-base) !important;
             border-color: transparent !important;
-            animation: pulse-glow 2s infinite;
+            font-weight: 600 !important;
+            animation: pulse-glow 2.5s ease-in-out infinite;
         }
         @keyframes pulse-glow {
-            0%  { box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.4); }
-            70% { box-shadow: 0 0 0 6px rgba(239, 68, 68, 0); }
-            100%{ box-shadow: 0 0 0 0 rgba(239, 68, 68, 0); }
+            0%, 100% { box-shadow: 0 0 0 0 rgba(212, 168, 75, 0.4); }
+            50%      { box-shadow: 0 0 0 4px rgba(212, 168, 75, 0); }
         }
 
         /* ── Scrollbar ─────────────────────────────── */
@@ -972,7 +1053,6 @@
         }
     </style>
 </head>
-
 <body>
 
 <!-- ════════════════════════════════════════════
@@ -981,17 +1061,17 @@
 <div class="sidebar">
     <div class="sidebar-header">
         <div class="brand">
-            <div class="brand-mark" style="background: none;">
-                <img src="/boomerang_logo.png" alt="Boomerang Logo" style="width: 22px; height: 22px; object-fit: contain;">
+            <div class="brand-mark">
+                <svg viewBox="0 0 24 24"><path d="M13 3L4 14h7l-2 7 9-11h-7l2-7z"/></svg>
             </div>
             <div class="brand-text">
-                <span class="brand-name" style="font-size: 15px;">Boomerang</span>
-                <span class="brand-sub">Platform</span>
+                <span class="brand-name">Boomerang</span>
+                <span class="brand-sub">Plataforma Financiera</span>
             </div>
         </div>
     </div>
 
-    <div class="nav-section-label">Espacio de trabajo</div>
+    <div class="nav-section-label">Espacio de Trabajo</div>
     <div class="nav-menu">
         <div class="nav-item active" onclick="switchView('appView', this)">
             <span class="nav-icon"><svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg></span>
@@ -1080,24 +1160,24 @@
                 </div>
             </div>
 
-            <div class="table-container">
-                <div class="table-toolbar">
-                    <div class="table-toolbar-title">Listado de Facturas</div>
-                    <div class="table-toolbar-actions">
-                        <button class="btn-success" onclick="openNewInvoiceModal()">
-                            <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
-                            Nueva Factura
-                        </button>
-                        <button class="btn-ghost" onclick="fetchData()" id="refreshBtn">
-                            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
-                            Refresh
-                        </button>
-                    </div>
+            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:10px;">
+                <div class="table-toolbar-title">Listado de Facturas</div>
+                <div class="table-toolbar-actions">
+                    <button class="btn-success" onclick="openNewInvoiceModal()">
+                        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
+                        Nueva Factura
+                    </button>
+                    <button class="btn-ghost" onclick="fetchData()" id="refreshBtn">
+                        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
+                        Refresh
+                    </button>
                 </div>
+            </div>
 
+            <div class="table-container">
                 <div id="loader" class="loader">
                     <div class="spinner"></div>
-                    <span class="loader-label">Cargando facturas...</span>
+                    <span class="loader-label">Cargando facturas…</span>
                 </div>
 
                 <table id="invoiceTable" style="display: none;">
@@ -1111,18 +1191,17 @@
                             <th>Cliente</th>
                             <th>Candidato</th>
                             <th>Posición</th>
-                            <th class="date-col-header">Fecha Factura</th>
-                            <th class="est-date-col-header">
+                            <th>Fecha Factura</th>
+                            <th>
                                 Fecha Est. Pago
                                 <span class="col-hint">Editable</span>
                             </th>
-                            <th class="pay-date-col-header">
+                            <th>Importe</th>
+                            <th>Estado</th>
+                            <th>
                                 Fecha Pago
                                 <span class="col-hint">Editable</span>
                             </th>
-                            <th>Importe</th>
-                            <th>Estado</th>
-                            <th>Acciones</th>
                         </tr>
                     </thead>
                     <tbody id="tableBody"></tbody>
@@ -1143,49 +1222,40 @@
 
             <div class="tip-banner">
                 <span class="tip-icon"><svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6"/></svg></span>
-                <span>Haz clic en un campo para editar y pulsa <strong style="color:var(--text-primary);">Guardar Cambios</strong> para actualizar.</span>
+                <span>Haz clic en cualquier campo para editarlo y pulsa <strong style="color:var(--text-primary);">Guardar</strong> para actualizar.</span>
             </div>
 
-            <div class="table-container">
-                <div class="table-toolbar">
-                    <div class="table-toolbar-title">Listado de Márgenes</div>
-                    <div class="table-toolbar-actions">
-                        <button class="btn-outline" onclick="saveAllMargins()" id="saveAllMarginsBtn" title="Guarda todos los cambios">
-                            💾 Guardar Cambios
-                        </button>
-                        <button class="btn-success" onclick="openNewInvoiceModal()">
-                            <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
-                            Nueva Factura
-                        </button>
-                        <button class="btn-ghost" onclick="fetchMarginData()" id="refreshMarginBtn">
-                            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
-                            Refresh
-                        </button>
-                    </div>
+            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:10px;">
+                <div class="table-toolbar-title">Listado de Márgenes</div>
+                <div class="table-toolbar-actions">
+                    <button class="btn-success" onclick="openNewInvoiceModal()">
+                        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
+                        Nueva Factura
+                    </button>
+                    <button class="btn-ghost" onclick="fetchMarginData()" id="refreshMarginBtn">
+                        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
+                        Refresh
+                    </button>
                 </div>
+            </div>
 
+            <div class="table-container">
                 <div id="marginLoader" class="loader"><div class="spinner"></div></div>
 
                 <table id="marginTable" style="display: none;">
                     <thead>
                         <tr>
-                            <th style="width: 40px;">
-                                <div class="checkbox-wrapper">
-                                    <input type="checkbox" id="selectAllMargin" onchange="toggleAllMargin(this)">
-                                </div>
-                            </th>
-                            <th style="display:none;">ID Factura</th>
+                            <th>ID</th>
                             <th>Cliente</th>
                             <th>Candidato</th>
                             <th>Reclutador</th>
-                            <th class="margin-key-col-header">Margen<span class="col-hint">Editable</span></th>
-                            <th class="margin-key-col-header">Com. Reclutador<span class="col-hint">Editable</span></th>
+                            <th class="margin-key-col-header">Margen <span class="date-hint">Key metric</span></th>
+                            <th class="margin-key-col-header">Com. Reclutador <span class="date-hint">Key metric</span></th>
                             <th>Cobrado BT</th>
                             <th>Ref.</th>
-                            <th class="margin-key-col-header">Importe Bruto<span class="col-hint">Editable</span></th>
+                            <th class="margin-key-col-header">Importe Bruto <span class="date-hint">Key metric</span></th>
                             <th>IVA</th>
-                            <th>Estado</th>
-                            <th class="pay-date-col-header">Fecha Pago</th>
+                            <th style="width: 60px;"></th>
                         </tr>
                     </thead>
                     <tbody id="marginTableBody"></tbody>
@@ -1209,78 +1279,45 @@
         <div class="container">
             <header>
                 <div>
-                    <div class="page-title">✨ Asistente Inteligente</div>
+                    <div class="page-title">Asistente Inteligente</div>
                     <div class="page-desc">Pregunta cualquier cosa sobre tus facturas, pagos o rendimiento</div>
                 </div>
             </header>
 
             <div class="chip-grid">
-                <button class="chip" onclick="fillChip(this.textContent)">💰 ¿Qué facturas están sin pagar?</button>
-                <button class="chip" onclick="fillChip(this.textContent)">👤 ¿Quién es nuestro mejor reclutador?</button>
-                <button class="chip" onclick="fillChip(this.textContent)">📅 ¿Qué vence para pago este mes?</button>
-                <button class="chip" onclick="fillChip(this.textContent)">📊 ¿Ingresos totales este trimestre?</button>
-                <button class="chip" onclick="fillChip(this.textContent)">⚠️ ¿Qué facturas están vencidas?</button>
-                <button class="chip" onclick="fillChip(this.textContent)">🔍 Facturas de un cliente específico</button>
+                <button class="chip" onclick="fillChip(this.textContent)">¿Qué facturas están sin pagar?</button>
+                <button class="chip" onclick="fillChip(this.textContent)">¿Quién es nuestro mejor reclutador?</button>
+                <button class="chip" onclick="fillChip(this.textContent)">¿Qué vence para pago este mes?</button>
+                <button class="chip" onclick="fillChip(this.textContent)">Ingresos totales este trimestre</button>
+                <button class="chip" onclick="fillChip(this.textContent)">¿Qué facturas están vencidas?</button>
+                <button class="chip" onclick="fillChip(this.textContent)">Facturas de un cliente específico</button>
             </div>
 
             <div class="chat-container">
                 <div id="chatHistory" style="flex: 1; overflow-y: auto; display: flex; flex-direction: column; gap: 12px; margin-bottom: 14px;">
-                    <div style="background: var(--accent-bg); border: 1px solid var(--accent-border); padding: 12px 14px; border-radius: var(--r-md); color: var(--text-secondary); align-self: flex-start; max-width: 80%; font-size: 13px; line-height: 1.6;">
-                        ✨ ¡Hola! Soy tu Asistente Inteligente, con tecnología Gemini AI. Puedo leer tus datos para responder preguntas al instante.<br><br>
-                        <span style="color: var(--text-tertiary); font-size: 12px;">Prueba uno de los ejemplos arriba o escribe tu propia pregunta abajo.</span>
+                    <div class="ai-welcome-msg">
+                        <div class="ai-avatar">
+                            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>
+                        </div>
+                        <div>
+                            <div style="font-weight: 550; color: var(--text-primary); margin-bottom: 4px; font-size: 13px;">Asistente Boomerang</div>
+                            <div style="color: var(--text-secondary); font-size: 13px; line-height: 1.6;">
+                                Puedo leer tus datos de facturación y responder preguntas al instante. Prueba uno de los ejemplos arriba o escribe tu propia pregunta.
+                            </div>
+                        </div>
                     </div>
                 </div>
                 <div class="chat-input-row">
-                    <input type="text" id="chatInput" placeholder="Escribe una pregunta o haz clic en un ejemplo..." onkeypress="handleChatKeyPress(event)">
-                    <button class="btn-primary" onclick="sendMessage()" id="sendBtn">✨ Preguntar</button>
+                    <input type="text" id="chatInput" placeholder="Escribe una pregunta…" onkeypress="handleChatKeyPress(event)">
+                    <button class="btn-primary" onclick="sendMessage()" id="sendBtn">
+                        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
+                        Enviar
+                    </button>
                 </div>
             </div>
         </div>
     </div>
 
-    <!-- ─── View 6: Configuration ─────────── -->
-    <div id="configView" class="view-container app-view">
-        <div class="container">
-            <header>
-                <div>
-                    <div class="page-title">⚙️ Configuración</div>
-                    <div class="page-desc">Gestiona tus integraciones de Google y el modelo de IA</div>
-                </div>
-            </header>
-
-            <div class="table-container" style="padding: 24px;">
-                <h3 style="margin-top: 0; margin-bottom: 20px; font-size: 14px; color: var(--text-primary);">🌐 Integración Google</h3>
-                <div style="background: var(--bg-hover); border: 1px solid var(--border); border-radius: var(--r-md); padding: 20px; margin-bottom: 28px;">
-                    <p style="color: var(--text-secondary); margin-top: 0; margin-bottom: 16px; line-height: 1.5; font-size: 13px;">
-                        Los paneles incrustados (Google Sheets y Looker Studio) utilizan la cuenta de Google actualmente activa en tu navegador, mientras que el backend usa una cuenta de servicio automática.
-                        <br><br>
-                        Si ves un mensaje de error o los paneles no cargan, probablemente necesitas cambiar tu cuenta de Google en el navegador.
-                    </p>
-                    <a href="https://accounts.google.com/AccountChooser" target="_blank" class="btn-primary" style="text-decoration: none; display: inline-flex;">
-                        👤 Cambiar Cuenta de Google (Login/Logout)
-                    </a>
-                </div>
-
-                <h3 style="margin-bottom: 20px; font-size: 14px; color: var(--text-primary);">🧠 Configuración Gemini AI</h3>
-                <div style="background: var(--bg-hover); border: 1px solid var(--border); border-radius: var(--r-md); padding: 20px;">
-                    <p style="color: var(--text-secondary); margin-top: 0; margin-bottom: 16px; line-height: 1.5; font-size: 13px;">
-                        Selecciona el modelo de Gemini que se utilizará para el <strong>Asistente Inteligente</strong>. Si un modelo falla, o es deprecado por Google, cambia a otro de la lista.
-                    </p>
-                    <label style="display: block; margin-bottom: 8px; font-weight: 500; font-size: 12px; color: var(--text-tertiary);">Modelo de IA Activo</label>
-                    <div style="display: flex; gap: 12px; align-items: center; max-width: 400px; margin-bottom: 20px;">
-                        <select id="geminiModelSelect" style="flex: 1; background: var(--bg-base); border: 1px solid var(--border); color: var(--text-primary); padding: 8px 10px; border-radius: var(--r-sm); font-size: 13px; font-family: inherit; outline: none;" onchange="saveGeminiModel()">
-                            <option value="gemini-2.5-flash">Gemini 2.5 Flash (Recomendado)</option>
-                            <option value="gemini-1.5-pro">Gemini 1.5 Pro</option>
-                            <option value="gemini-1.5-flash">Gemini 1.5 Flash</option>
-                            <option value="gemini-pro">Gemini Pro</option>
-                        </select>
-                    </div>
-                    <button class="btn-success" onclick="saveGeminiModel()">💾 Guardar Preferencia</button>
-                </div>
-            </div>
-        </div>
-    </div>
-
 </div>
 
 <!-- ════════════════════════════════════════════
@@ -1288,7 +1325,7 @@
      ════════════════════════════════════════════ -->
 <div id="newInvoiceModal" class="modal-overlay">
     <div class="modal">
-        <h2>➕ Crear Nueva Factura</h2>
+        <h2>Crear Nueva Factura</h2>
         <div class="modal-field">
             <label>Nombre Cliente *</label>
             <input type="text" id="newClientName" placeholder="ej. ACME Corp">
@@ -1305,7 +1342,7 @@
             <label>Fecha Inicio</label>
             <input type="date" id="newStartDate">
         </div>
-        <div class="modal-field" style="display: none;">
+        <div class="modal-field">
             <label>Salario Fijo (€)</label>
             <input type="number" id="newFixSalary" step="0.01" placeholder="ej. 50000">
         </div>
@@ -1313,7 +1350,7 @@
             <label>Fecha Factura</label>
             <input type="date" id="newInvoiceDate">
         </div>
-        <div class="modal-field" style="display: none;">
+        <div class="modal-field">
             <label>Tarifa %</label>
             <input type="text" id="newFeePercent" placeholder="ej. 20%">
         </div>
@@ -1328,18 +1365,19 @@
     </div>
 </div>
 
-
+<!-- ════════════════════════════════════════════
+     JAVASCRIPT — All logic preserved
+     ════════════════════════════════════════════ -->
 <script>
     // ===== Navigation =====
     function switchView(viewId, element) {
         document.querySelectorAll('.nav-item').forEach(el => el.classList.remove('active'));
         element.classList.add('active');
-
         document.querySelectorAll('.view-container').forEach(el => el.classList.remove('active'));
         document.getElementById(viewId).classList.add('active');
     }
 
-    // ===== Toast Notifications =====
+    // ===== Toast =====
     function showToast(message, type = 'success') {
         const toast = document.createElement('div');
         toast.className = `toast ${type}`;
@@ -1354,39 +1392,17 @@
     let currentFilter = 'all';
 
     function toggleFilter(filter) {
-        if (currentFilter === filter) {
-            currentFilter = 'all';
-        } else {
-            currentFilter = filter;
-        }
+        currentFilter = currentFilter === filter ? 'all' : filter;
 
-        const rows = document.querySelectorAll('.invoice-row');
-        rows.forEach(row => {
-            if (currentFilter === 'all') {
-                row.style.display = '';
-            } else {
-                row.style.display = row.classList.contains(currentFilter) ? '' : 'none';
-            }
+        document.querySelectorAll('.invoice-row').forEach(row => {
+            row.style.display = (currentFilter === 'all' || row.classList.contains(currentFilter)) ? '' : 'none';
         });
 
-        const cards = document.querySelectorAll('#statsBar .stat-card');
-        cards.forEach(card => {
-            if (currentFilter === 'all') {
-                card.style.opacity = '1';
-                card.style.transform = 'scale(1)';
-                card.style.borderColor = 'var(--border-color)';
-            } else {
-                if (card.classList.contains(currentFilter)) {
-                    card.style.opacity = '1';
-                    card.style.transform = 'scale(1.05)';
-                    const color = window.getComputedStyle(card.querySelector('.stat-card-value')).color;
-                    card.style.borderColor = color;
-                } else {
-                    card.style.opacity = '0.5';
-                    card.style.transform = 'scale(0.95)';
-                    card.style.borderColor = 'var(--border-color)';
-                }
-            }
+        document.querySelectorAll('#statsBar .stat-card').forEach(card => {
+            card.classList.remove('filter-active', 'filter-dimmed');
+            if (currentFilter === 'all') return;
+            if (card.classList.contains(currentFilter)) card.classList.add('filter-active');
+            else card.classList.add('filter-dimmed');
         });
     }
 
@@ -1394,837 +1410,324 @@
         let hasUnsaved = false;
         document.querySelectorAll('.invoice-row').forEach(tr => {
             let rowUnsaved = false;
-            const dateInput = tr.querySelector('.date-input');
-            const estDateInput = tr.querySelector('.est-date-input');
-            const statusSelect = tr.querySelector('.status-select');
-            
-            if (dateInput && dateInput.value !== dateInput.getAttribute('data-original')) {
-                rowUnsaved = true;
-            }
-            if (estDateInput && estDateInput.value !== estDateInput.getAttribute('data-original')) {
-                rowUnsaved = true;
-            }
-            if (statusSelect && statusSelect.value !== statusSelect.getAttribute('data-original')) {
-                rowUnsaved = true;
-            }
-            
-            if (rowUnsaved) {
-                tr.classList.add('unsaved');
-                hasUnsaved = true;
-            } else {
-                tr.classList.remove('unsaved');
-            }
+            ['date-input', 'est-date-input', 'status-select'].forEach(cls => {
+                const el = tr.querySelector('.' + cls);
+                if (el && el.value !== el.getAttribute('data-original')) rowUnsaved = true;
+            });
+            tr.classList.toggle('unsaved', rowUnsaved);
+            if (rowUnsaved) hasUnsaved = true;
         });
-        
         const saveBtn = document.getElementById('saveFieldsBtn');
-        if (saveBtn) {
-            if (hasUnsaved) {
-                saveBtn.classList.add('pulse-red');
-            } else {
-                saveBtn.classList.remove('pulse-red');
-            }
-        }
+        if (saveBtn) saveBtn.classList.toggle('pulse-red', hasUnsaved);
     }
 
-    // ===== Invoice Upload Logic =====
+    // ===== Invoice Logic =====
     async function fetchData() {
         const loader = document.getElementById('loader');
         const table = document.getElementById('invoiceTable');
         const tbody = document.getElementById('tableBody');
         const refreshBtn = document.getElementById('refreshBtn');
-        
-        refreshBtn.disabled = true;
-        loader.style.display = 'flex';
-        table.style.display = 'none';
-        tbody.innerHTML = '';
-
+        refreshBtn.disabled = true; loader.style.display = 'flex'; table.style.display = 'none'; tbody.innerHTML = '';
         try {
             const response = await fetch('/api/invoices');
             const data = await response.json();
-            
-            if (response.ok) {
-                // Sort ascending by Fecha Factura
-                data.sort((a, b) => {
-                    const parseDate = (d) => {
-                        if (!d) return 0;
-                        const p = d.split('/');
-                        if (p.length === 3) return new Date(`${p[2]}-${p[1]}-${p[0]}`).getTime();
-                        return new Date(d).getTime() || 0;
-                    };
-                    return parseDate(a['Fecha Factura']) - parseDate(b['Fecha Factura']);
-                });
-                
-                globalData = data;
-                renderTable(data);
-                table.style.display = 'table';
-            } else {
-                alert('Error al obtener datos: ' + data.error);
-            }
-        } catch (error) {
-            console.error('Error al cargar:', error);
-            alert('Error al conectar con el servidor.');
-        } finally {
-            loader.style.display = 'none';
-            refreshBtn.disabled = false;
-            document.getElementById('selectAll').checked = false;
-        }
+            if (response.ok) { globalData = data; renderTable(data); table.style.display = 'table'; }
+            else alert('Error: ' + data.error);
+        } catch (e) { console.error(e); alert('Error al conectar.'); }
+        finally { loader.style.display = 'none'; refreshBtn.disabled = false; document.getElementById('selectAll').checked = false; }
     }
 
+    function fmtDate(raw) {
+        if (!raw) return '';
+        if (raw.includes('/')) { const p = raw.split('/'); return p.length === 3 ? `${p[2]}-${p[1]}-${p[0]}` : raw; }
+        return raw;
+    }
+
     function renderTable(data) {
         const tbody = document.getElementById('tableBody');
         tbody.innerHTML = '';
-        
-        data.forEach((invoice, index) => {
-            const originalDateStr = invoice['Fecha Factura'] || '';
-            let dateVal = originalDateStr;
-            let formattedDate = "";
-            
-            if (dateVal && dateVal.includes('/')) {
-                const parts = dateVal.split('/');
-                if (parts.length === 3) {
-                    formattedDate = `${parts[2]}-${parts[1]}-${parts[0]}`;
-                }
-            } else {
-                formattedDate = dateVal;
-            }
+        data.forEach((inv, i) => {
+            const fd = fmtDate(inv['Fecha Factura'] || '');
+            const epd = fmtDate(inv['Estimated Payment Date'] || '');
+            const pd = fmtDate(inv['Payment Date'] || '');
+            const st = inv['Status'] || '';
+            const sc = /paid/i.test(st) ? 'paid' : /overdue/i.test(st) ? 'overdue' : 'pending';
 
-            const sheetStatus = invoice['Status'] || '';
-            let statusClass = '';
-            if (/paid/i.test(sheetStatus)) {
-                statusClass = 'paid';
-            } else if (/overdue/i.test(sheetStatus)) {
-                statusClass = 'overdue';
-            } else {
-                statusClass = 'pending';
-            }
+            let did = '';
+            if (fd && fd.includes('-')) { const p = fd.split('-'); did = `${p[0]}${p[1].padStart(2,'0')}${p[2].padStart(2,'0')}-${String(i+1).padStart(2,'0')}`; }
+            if (!did) did = `INV-${String(i+1).padStart(2,'0')}`;
 
-            // Estimated Payment Date (col T)
-            const estPayRaw = invoice['Estimated Payment Date'] || '';
-            let estPayFormatted = '';
-            if (estPayRaw && estPayRaw.includes('/')) {
-                const ep = estPayRaw.split('/');
-                if (ep.length === 3) estPayFormatted = `${ep[2]}-${ep[1]}-${ep[0]}`;
-            } else {
-                estPayFormatted = estPayRaw;
-            }
-
-            // Actual Payment Date (col U)
-            const payRaw = invoice['Payment Date'] || '';
-            let payFormatted = '';
-            if (payRaw && payRaw.includes('/')) {
-                const pp = payRaw.split('/');
-                if (pp.length === 3) payFormatted = `${pp[2]}-${pp[1]}-${pp[0]}`;
-            } else {
-                payFormatted = payRaw;
-            }
-
-            // Build the Invoice display ID like 20260301-01 based on date and index
-            let displayInvoiceId = '';
-            const dt = formattedDate; // YYYY-MM-DD
-            if (dt && dt.includes('-')) {
-                const parts = dt.split('-');
-                if (parts.length === 3) {
-                    const yyyy = parts[0];
-                    const mm = parts[1].padStart(2, '0');
-                    const dd = parts[2].padStart(2, '0');
-                    const seq = String(index + 1).padStart(2, '0');
-                    displayInvoiceId = `${yyyy}${mm}${dd}-${seq}`;
-                }
-            }
-            if (!displayInvoiceId) {
-                displayInvoiceId = `REQ-${String(index + 1).padStart(2, '0')}`;
-            }
-
             const tr = document.createElement('tr');
-            tr.className = `invoice-row ${statusClass}`;
-            tr.style.display = (currentFilter === 'all' || currentFilter === statusClass) ? '' : 'none';
+            tr.className = `invoice-row ${sc}`;
+            tr.style.display = (currentFilter === 'all' || currentFilter === sc) ? '' : 'none';
             tr.innerHTML = `
-                <td>
-                    <div class="checkbox-wrapper">
-                        <input type="checkbox" class="row-checkbox" value="${index}">
-                    </div>
+                <td><div class="checkbox-wrapper"><input type="checkbox" class="row-checkbox" value="${i}"></div></td>
+                <td style="color:var(--text-primary);font-weight:500;">
+                    <span style="color:var(--text-ghost);font-size:10.5px;font-family:'JetBrains Mono',monospace;margin-right:6px;">${did}</span>
+                    ${inv['Cliente']}
                 </td>
-                <td style="font-weight: 500;">
-                    <span style="color: var(--text-muted); font-size: 0.8rem; margin-right: 8px; font-family: monospace;">${displayInvoiceId}</span>
-                    ${invoice['Cliente']}
-                </td>
-                <td>${invoice['Candidato']}</td>
-                <td style="color: var(--text-muted);">${invoice['Proceso']}</td>
+                <td>${inv['Candidato']}</td>
+                <td style="color:var(--text-tertiary);">${inv['Proceso']}</td>
+                <td><input type="date" class="date-input" id="date-${i}" value="${fd}" data-original="${fd}" data-row="${inv['_sheet_row_index']}" onchange="checkUnsavedChanges()"></td>
+                <td><input type="date" class="est-date-input" id="estdate-${i}" value="${epd}" data-original="${epd}" data-row="${inv['_sheet_row_index']}" onchange="checkUnsavedChanges()"></td>
+                <td class="currency">€ ${(inv['Importe factura']||0).toLocaleString('es-ES',{minimumFractionDigits:0,maximumFractionDigits:0})}</td>
                 <td>
-                    <input type="date" class="date-input" id="date-${index}" value="${formattedDate}" 
-                           data-original="${formattedDate}" data-row="${invoice['_sheet_row_index']}" onchange="checkUnsavedChanges()">
-                </td>
-                <td>
-                    <input type="date" class="est-date-input" id="estdate-${index}" value="${estPayFormatted}"
-                           data-original="${estPayFormatted}" data-row="${invoice['_sheet_row_index']}" onchange="checkUnsavedChanges()">
-                </td>
-                <td>
-                    <input type="date" class="date-input" id="paydate-${index}" value="${payFormatted}" style="background: rgba(16, 185, 129, 0.08) !important; border-color: rgba(16, 185, 129, 0.4) !important;"
-                           data-original="${payFormatted}" data-row="${invoice['_sheet_row_index']}" onchange="checkUnsavedChanges()">
-                </td>
-                <td class="currency">€ ${(invoice['Importe factura'] || 0).toLocaleString('es-ES', {minimumFractionDigits: 0, maximumFractionDigits: 0})}</td>
-                <td>
-                    <select class="status-select ${statusClass}" id="status-${index}"
-                            data-original="${sheetStatus}" data-row="${invoice['_sheet_row_index']}"
-                            onchange="updateStatusSelectStyle(this); checkUnsavedChanges()">
-                        <option value="Pending" ${sheetStatus === 'Pending' || sheetStatus === '' ? 'selected' : ''}>Pendiente</option>
-                        <option value="Paid" ${/paid/i.test(sheetStatus) && !/overdue/i.test(sheetStatus) ? 'selected' : ''}>Pagada</option>
-                        <option value="Overdue" ${/overdue/i.test(sheetStatus) ? 'selected' : ''}>Vencida</option>
-                        <option value="Cancelled" ${/cancelled/i.test(sheetStatus) ? 'selected' : ''}>Cancelada</option>
+                    <select class="status-select ${sc}" id="status-${i}" data-original="${st}" data-row="${inv['_sheet_row_index']}" onchange="updateStatusSelectStyle(this);checkUnsavedChanges()">
+                        <option value="Pending" ${st==='Pending'||st===''?'selected':''}>Pendiente</option>
+                        <option value="Paid" ${/paid/i.test(st)&&!/overdue/i.test(st)?'selected':''}>Pagada</option>
+                        <option value="Overdue" ${/overdue/i.test(st)?'selected':''}>Vencida</option>
+                        <option value="Cancelled" ${/cancelled/i.test(st)?'selected':''}>Cancelada</option>
                     </select>
                 </td>
-                <td>
-                    <div style="display: flex; gap: 8px; flex-direction: column;">
-                        <button class="btn-outline" style="padding: 6px 12px; font-size: 0.8rem;" onclick="uploadSingle(${index})" id="btn-${index}">
-                            ☁️ Enviar a Biloop
-                        </button>
-                        <button class="btn-primary" style="padding: 6px 12px; font-size: 0.8rem;" onclick="saveAndDownloadInvoice(${index})" id="btn-save-${index}">
-                            💾 Guardar Factura
-                        </button>
-                    </div>
-                </td>
-            `;
+                <td><input type="date" class="pay-date-input" id="paydate-${i}" value="${pd}" data-original="${pd}" data-row="${inv['_sheet_row_index']}" onchange="checkUnsavedChanges()"></td>`;
             tbody.appendChild(tr);
         });
-
         updateStatsBar(data);
-        document.getElementById('statsBar').style.display = 'flex';
+        document.getElementById('statsBar').style.display = 'grid';
         document.getElementById('workflowHint').style.display = 'flex';
         checkUnsavedChanges();
     }
 
-    function toggleAll(source) {
-        const checkboxes = document.querySelectorAll('.row-checkbox');
-        checkboxes.forEach(cb => cb.checked = source.checked);
-    }
+    function toggleAll(s) { document.querySelectorAll('.row-checkbox').forEach(cb => cb.checked = s.checked); }
 
     function updateStatsBar(data) {
-        const total = data.length;
-        const totalAmt = data.reduce((sum, inv) => sum + (inv['Importe factura'] || 0), 0);
-        const paid    = data.filter(inv => /paid/i.test(inv['Status']) && !/overdue/i.test(inv['Status'])).length;
-        const overdue = data.filter(inv => /overdue/i.test(inv['Status'])).length;
-        const pending = total - paid - overdue;
-
-
-        document.getElementById('statPaid').textContent    = paid;
-        document.getElementById('statPending').textContent = pending;
+        const paid = data.filter(i => /paid/i.test(i['Status']) && !/overdue/i.test(i['Status'])).length;
+        const overdue = data.filter(i => /overdue/i.test(i['Status'])).length;
+        document.getElementById('statPaid').textContent = paid;
+        document.getElementById('statPending').textContent = data.length - paid - overdue;
         document.getElementById('statOverdue').textContent = overdue;
     }
 
-    // Update status select dropdown colour class on change
-    function updateStatusSelectStyle(select) {
-        select.classList.remove('paid', 'overdue', 'pending');
-        const val = select.value.toLowerCase();
-        if (val === 'paid') select.classList.add('paid');
-        else if (val === 'overdue') select.classList.add('overdue');
-        else select.classList.add('pending');
+    function updateStatusSelectStyle(sel) {
+        sel.classList.remove('paid','overdue','pending');
+        const v = sel.value.toLowerCase();
+        sel.classList.add(v === 'paid' ? 'paid' : v === 'overdue' ? 'overdue' : 'pending');
     }
 
-    // Save changed status and/or estimated payment dates
+    function dateToSheet(val) {
+        if (!val) return '';
+        const p = val.split('-');
+        return p.length === 3 ? `${p[2]}/${p[1]}/${p[0]}` : val;
+    }
+
     async function saveInvoiceFields() {
-        const statusSelects = document.querySelectorAll('.status-select');
-        const estDateInputs = document.querySelectorAll('.est-date-input');
-        const dateInputs = document.querySelectorAll('.date-input');
-        
-        // Build a map: row_index -> { status?, est_payment_date?, invoice_date? }
         const rowMap = {};
-
-        statusSelects.forEach(sel => {
-            const original = sel.getAttribute('data-original');
-            const current = sel.value;
-            const rowIdx = sel.getAttribute('data-row');
-            if (current !== original) {
-                if (!rowMap[rowIdx]) rowMap[rowIdx] = { row_index: parseInt(rowIdx) };
-                rowMap[rowIdx].status = current;
+        document.querySelectorAll('.status-select').forEach(sel => {
+            if (sel.value !== sel.getAttribute('data-original')) {
+                const r = sel.getAttribute('data-row');
+                if (!rowMap[r]) rowMap[r] = { row_index: parseInt(r) };
+                rowMap[r].status = sel.value;
             }
         });
-
-        estDateInputs.forEach(input => {
-            const original = input.getAttribute('data-original');
-            const current = input.value;
-            const rowIdx = input.getAttribute('data-row');
-            if (current !== original) {
-                // Convert YYYY-MM-DD to DD/MM/YYYY for the sheet
-                let sheetDate = current;
-                if (current) {
-                    const parts = current.split('-');
-                    if (parts.length === 3) sheetDate = `${parts[2]}/${parts[1]}/${parts[0]}`;
-                }
-                if (!rowMap[rowIdx]) rowMap[rowIdx] = { row_index: parseInt(rowIdx) };
-                rowMap[rowIdx].est_payment_date = sheetDate;
+        document.querySelectorAll('.est-date-input').forEach(inp => {
+            if (inp.value !== inp.getAttribute('data-original')) {
+                const r = inp.getAttribute('data-row');
+                if (!rowMap[r]) rowMap[r] = { row_index: parseInt(r) };
+                rowMap[r].est_payment_date = dateToSheet(inp.value);
             }
         });
-
-        dateInputs.forEach(input => {
-            const original = input.getAttribute('data-original');
-            const current = input.value;
-            const rowIdx = input.getAttribute('data-row');
-            if (current !== original) {
-                // Convert YYYY-MM-DD to DD/MM/YYYY for the sheet
-                let sheetDate = current;
-                if (current) {
-                    const parts = current.split('-');
-                    if (parts.length === 3) sheetDate = `${parts[2]}/${parts[1]}/${parts[0]}`;
-                }
-                if (!rowMap[rowIdx]) rowMap[rowIdx] = { row_index: parseInt(rowIdx) };
-                
-                if (input.id.startsWith('paydate-')) {
-                    rowMap[rowIdx].payment_date = sheetDate;
-                } else if (input.id.startsWith('date-')) {
-                    rowMap[rowIdx].invoice_date = sheetDate;
-                }
+        document.querySelectorAll('.date-input').forEach(inp => {
+            if (inp.value !== inp.getAttribute('data-original')) {
+                const r = inp.getAttribute('data-row');
+                if (!rowMap[r]) rowMap[r] = { row_index: parseInt(r) };
+                rowMap[r].invoice_date = dateToSheet(inp.value);
             }
         });
-
         const updates = Object.values(rowMap);
+        if (!updates.length) { showToast('No hay cambios para guardar.', 'error'); return; }
 
-        if (updates.length === 0) {
-            showToast('No hay cambios en el estado o fecha estimada para guardar.', 'error');
-            return;
-        }
-
         const btn = document.getElementById('saveFieldsBtn');
-        btn.disabled = true;
-        btn.innerHTML = '⏳ Guardando...';
-
+        btn.disabled = true; btn.textContent = 'Guardando…';
         try {
-            const response = await fetch('/api/update_invoice_fields', {
-                method: 'POST',
-                headers: { 'Content-Type': 'application/json' },
-                body: JSON.stringify(updates)
-            });
-            const result = await response.json();
-
-            if (response.ok && result.success) {
-                // Mark saved values as the new originals
-                statusSelects.forEach(sel => sel.setAttribute('data-original', sel.value));
-                estDateInputs.forEach(input => input.setAttribute('data-original', input.value));
-                dateInputs.forEach(input => input.setAttribute('data-original', input.value));
+            const res = await fetch('/api/update_invoice_fields', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(updates) });
+            const result = await res.json();
+            if (res.ok && result.success) {
+                document.querySelectorAll('.status-select').forEach(s => s.setAttribute('data-original', s.value));
+                document.querySelectorAll('.est-date-input').forEach(i => i.setAttribute('data-original', i.value));
+                document.querySelectorAll('.date-input').forEach(i => i.setAttribute('data-original', i.value));
                 checkUnsavedChanges();
-                showToast(`${updates.length} fila(s) actualizada(s) con éxito!`);
-                // Refresh sheets iframe
-                const sheetsIframe = document.querySelector('#sheetsView iframe');
-                if (sheetsIframe) sheetsIframe.src = sheetsIframe.src;
-            } else {
-                showToast('Error al guardar: ' + result.message, 'error');
-            }
-        } catch (error) {
-            showToast('Error al conectar con el servidor', 'error');
-        } finally {
+                showToast(`${updates.length} fila(s) actualizada(s)`);
+                const sf = document.querySelector('#sheetsView iframe'); if (sf) sf.src = sf.src;
+            } else showToast('Error: ' + result.message, 'error');
+        } catch (e) { showToast('Error de conexión', 'error'); }
+        finally {
             btn.disabled = false;
-            btn.innerHTML = '✏️ Guardar Cambios';
+            btn.innerHTML = '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg> Guardar Cambios';
         }
     }
 
-
     async function uploadInvoice(index, downloadPdf = false) {
-        const invoiceData = globalData[index];
-        const statusBadge = document.getElementById(`status-${index}`);
+        const inv = globalData[index];
+        const badge = document.getElementById(`status-${index}`);
         const btn = document.getElementById(`btn-${index}`);
-        
-        statusBadge.className = 'status-badge';
-        statusBadge.innerText = 'Subiendo...';
-        btn.disabled = true;
-        
+        if (badge) { badge.className = 'status-badge'; badge.innerText = 'Uploading…'; }
+        if (btn) btn.disabled = true;
         try {
-            const reqBody = { ...invoiceData, downloadPdf };
-            const response = await fetch('/api/upload', {
-                method: 'POST',
-                headers: { 'Content-Type': 'application/json' },
-                body: JSON.stringify(reqBody)
-            });
-            
-            const result = await response.json();
-            
-            if (response.ok && result.success) {
-                statusBadge.className = 'status-badge success';
-                statusBadge.innerText = 'Subido ✓';
-                btn.innerText = 'Hecho';
-                return result;
+            const res = await fetch('/api/upload', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...inv, downloadPdf }) });
+            const r = await res.json();
+            if (res.ok && r.success) {
+                if (badge) { badge.className = 'status-badge success'; badge.innerText = 'Done'; }
+                if (btn) btn.innerText = 'Done'; return r;
             } else {
-                statusBadge.className = 'status-badge error';
-                statusBadge.innerText = 'Error ✗';
-                btn.disabled = false;
-                btn.innerText = 'Reintentar';
-                return null;
+                if (badge) { badge.className = 'status-badge error'; badge.innerText = 'Error'; }
+                if (btn) { btn.disabled = false; btn.innerText = 'Retry'; } return null;
             }
-        } catch (error) {
-            statusBadge.className = 'status-badge error';
-            statusBadge.innerText = 'Error ✗';
-            btn.disabled = false;
-            return null;
+        } catch (e) {
+            if (badge) { badge.className = 'status-badge error'; badge.innerText = 'Error'; }
+            if (btn) btn.disabled = false; return null;
         }
     }
 
-    function uploadSingle(index) {
-        if (!confirm('¿Estás seguro de que quieres enviar esta factura a Biloop?')) return;
-        uploadInvoice(index);
-    }
+    function uploadSingle(i) { if (confirm('¿Enviar esta factura a Biloop?')) uploadInvoice(i); }
 
-    async function saveAndDownloadInvoice(index) {
-        if (!confirm('¿Estás seguro de que quieres guardar y descargar esta factura de Biloop?')) return;
-        const btnSave = document.getElementById(`btn-save-${index}`);
-        const originalText = btnSave.innerHTML;
-        btnSave.disabled = true;
-        btnSave.innerHTML = 'Guardando...';
-
+    async function saveAndDownloadInvoice(i) {
+        if (!confirm('¿Guardar y descargar esta factura de Biloop?')) return;
+        const btn = document.getElementById(`btn-save-${i}`); const orig = btn.innerHTML;
+        btn.disabled = true; btn.innerHTML = 'Guardando…';
         try {
-            // 1. Send data to Biloop with downloadPdf flag = true
-            const result = await uploadInvoice(index, true);
-
-            // 2. Download invoice if successful and pdf is present
-            if (result && result.success) {
-                if (result.pdfBase64) {
-                    const link = document.createElement('a');
-                    link.href = `data:application/pdf;base64,${result.pdfBase64}`;
-                    link.download = result.fileName || `Factura_${globalData[index].Cliente}.pdf`;
-                    document.body.appendChild(link);
-                    link.click();
-                    document.body.removeChild(link);
-                    showToast("Factura descargada con éxito.", "success");
-                } else {
-                    showToast("Factura enviada, pero no tiene PDF disponible.", "warning");
-                }
-            } else {
-                showToast("Fallo al guardar la factura en Biloop.", "error");
-            }
-        } catch (e) {
-            console.error(e);
-            showToast("Error inesperado en saveAndDownloadInvoice.", "error");
-        }
-
-        btnSave.innerHTML = originalText;
-        btnSave.disabled = false;
+            const r = await uploadInvoice(i, true);
+            if (r?.success && r.pdfBase64) {
+                const a = document.createElement('a'); a.href = `data:application/pdf;base64,${r.pdfBase64}`;
+                a.download = r.fileName || `Factura_${globalData[i].Cliente}.pdf`;
+                document.body.appendChild(a); a.click(); document.body.removeChild(a);
+                showToast('Factura descargada');
+            } else showToast(r?.success ? 'Sin PDF disponible' : 'Error al subir', 'error');
+        } catch (e) { showToast('Error inesperado', 'error'); }
+        btn.innerHTML = orig; btn.disabled = false;
     }
 
     async function uploadSelected() {
-        const checkboxes = document.querySelectorAll('.row-checkbox:checked');
-        const uploadBtn = document.getElementById('uploadBtn');
-        
-        if (checkboxes.length === 0) {
-            alert('Por favor, selecciona al menos una factura para subir.');
-            return;
+        const cbs = document.querySelectorAll('.row-checkbox:checked');
+        const btn = document.getElementById('uploadBtn');
+        if (!cbs.length) { showToast('Selecciona al menos una factura.', 'error'); return; }
+        if (!confirm(`¿Enviar ${cbs.length} factura(s) a Biloop?`)) return;
+        btn.disabled = true; const orig = btn.innerHTML; btn.textContent = 'Subiendo…';
+        for (const cb of cbs) {
+            await uploadInvoice(cb.value);
+            const s = document.getElementById(`status-${cb.value}`);
+            if (s?.className.includes('success')) cb.checked = false;
         }
-        
-        if (!confirm(`¿Estás seguro de que quieres enviar ${checkboxes.length} factura(s) a Biloop?`)) return;
-        
-        uploadBtn.disabled = true;
-        const originalText = uploadBtn.innerHTML;
-        uploadBtn.innerHTML = 'Subiendo...';
-        
-        for (let i = 0; i < checkboxes.length; i++) {
-            const index = checkboxes[i].value;
-            await uploadInvoice(index);
-            if(document.getElementById(`status-${index}`).className.includes('success')) {
-                checkboxes[i].checked = false;
-            }
-        }
-        
-        uploadBtn.disabled = false;
-        uploadBtn.innerHTML = originalText;
-        document.getElementById('selectAll').checked = false;
+        btn.disabled = false; btn.innerHTML = orig; document.getElementById('selectAll').checked = false;
     }
 
-    // ===== Margin Management Logic =====
+    // ===== Margins =====
     async function fetchMarginData() {
-        const loader = document.getElementById('marginLoader');
-        const table = document.getElementById('marginTable');
-        const tbody = document.getElementById('marginTableBody');
-        
-        loader.style.display = 'flex';
-        table.style.display = 'none';
-        tbody.innerHTML = '';
-
+        const loader = document.getElementById('marginLoader'), table = document.getElementById('marginTable'), tbody = document.getElementById('marginTableBody');
+        loader.style.display = 'flex'; table.style.display = 'none'; tbody.innerHTML = '';
         try {
-            const response = await fetch('/api/margins');
-            const data = await response.json();
-            if (response.ok && !data.error) {
-                marginData = data;
-                renderMarginTable(data);
-                table.style.display = 'table';
-            } else {
-                console.error('Error fetching margins:', data.error);
-            }
-        } catch (error) {
-            console.error('Failed to fetch margins:', error);
-        } finally {
-            loader.style.display = 'none';
-        }
+            const res = await fetch('/api/margins'); const data = await res.json();
+            if (res.ok && !data.error) { marginData = data; renderMarginTable(data); table.style.display = 'table'; }
+        } catch (e) { console.error(e); }
+        finally { loader.style.display = 'none'; }
     }
 
-    const MARGIN_KEYS = [
-        'Recruiter Name', 'Margin', 'Recruiter Commission', 'Collected by BT',
-        'Invoice', 'VAT', 'Gross Invoice Amount (Recruiter)'
-    ];
+    const MARGIN_KEYS = ['Recruiter Name','Margin','Recruiter Commission','Collected by BT','Invoice','VAT','Gross Invoice Amount (Recruiter)'];
+    const MARGIN_KEY_HIGHLIGHT = new Set(['Margin','Recruiter Commission','Gross Invoice Amount (Recruiter)']);
 
-    // Columns to highlight as key calculation metrics
-    const MARGIN_KEY_HIGHLIGHT = new Set(['Margin', 'Recruiter Commission', 'Gross Invoice Amount (Recruiter)']);
-
     function renderMarginTable(data) {
         const tbody = document.getElementById('marginTableBody');
         tbody.innerHTML = '';
-
-        data.forEach((record, index) => {
+        data.forEach((rec, i) => {
             const tr = document.createElement('tr');
-            tr.className = 'margin-invoice-row';
-            
             let html = `
-                <td>
-                    <div class="checkbox-wrapper">
-                        <input type="checkbox" class="margin-row-checkbox" value="${index}">
-                    </div>
-                </td>
-                <td style="font-weight: 600; color: var(--primary); white-space: nowrap; display: none;">${record._invoice_id}</td>
-                <td style="white-space: nowrap;">${record._client_name}</td>
-                <td style="white-space: nowrap;">${record._candidate_name}</td>
-            `;
-            
-            // Editable margin columns
-            MARGIN_KEYS.forEach(key => {
-                const val = record[key] || '';
-                const isKey = MARGIN_KEY_HIGHLIGHT.has(key);
-                const cellStyle = isKey ? ' style="background: rgba(37,99,235,0.05);"' : '';
-                const inputClass = isKey ? 'margin-input margin-input-key' : 'margin-input';
-                html += `<td${cellStyle}><input class="${inputClass}" data-row="${index}" data-key="${key}" data-original="${val.replace(/"/g, '&quot;')}" value="${val.replace(/"/g, '&quot;')}" onchange="checkMarginUnsavedChanges()"></td>`;
+                <td style="font-weight:500;color:var(--accent);font-family:'JetBrains Mono',monospace;font-size:11px;">${rec._invoice_id}</td>
+                <td>${rec._client_name}</td><td>${rec._candidate_name}</td>`;
+            MARGIN_KEYS.forEach(k => {
+                const v = rec[k] || '';
+                const cls = MARGIN_KEY_HIGHLIGHT.has(k) ? 'margin-input margin-input-key' : 'margin-input';
+                html += `<td><input class="${cls}" data-row="${i}" data-key="${k}" value="${v.replace(/"/g,'&quot;')}"></td>`;
             });
-            
-            const sheetStatus = record._status || '';
-            let statusClass = '';
-            if (/paid/i.test(sheetStatus)) statusClass = 'paid';
-            else if (/overdue/i.test(sheetStatus)) statusClass = 'overdue';
-            else statusClass = 'pending';
-
-            html += `
-                <td>
-                    <select class="status-select margin-status-select ${statusClass}" data-row="${index}" data-original="${sheetStatus}" onchange="updateStatusSelectStyle(this); checkMarginUnsavedChanges()">
-                        <option value="Pending" ${sheetStatus === 'Pending' || sheetStatus === '' ? 'selected' : ''}>Pendiente</option>
-                        <option value="Paid" ${/paid/i.test(sheetStatus) && !/overdue/i.test(sheetStatus) ? 'selected' : ''}>Pagada</option>
-                        <option value="Overdue" ${/overdue/i.test(sheetStatus) ? 'selected' : ''}>Vencida</option>
-                        <option value="Cancelled" ${/cancelled/i.test(sheetStatus) ? 'selected' : ''}>Cancelada</option>
-                    </select>
-                </td>
-            `;
-
-            const payRaw = record._payment_date || '';
-            let payFormatted = '';
-            if (payRaw && payRaw.includes('/')) {
-                const pp = payRaw.split('/');
-                if (pp.length === 3) payFormatted = `${pp[2]}-${pp[1]}-${pp[0]}`;
-            } else {
-                payFormatted = payRaw;
-            }
-
-            html += `
-                <td>
-                    <input type="date" class="date-input margin-paydate-input" data-row="${index}" value="${payFormatted}" style="background: rgba(16, 185, 129, 0.08) !important; border-color: rgba(16, 185, 129, 0.4) !important;"
-                           data-original="${payFormatted}" onchange="checkMarginUnsavedChanges()">
-                </td>
-            `;
-            
-            tr.innerHTML = html;
-            tbody.appendChild(tr);
+            html += `<td><button class="margin-save-btn" onclick="saveMarginRow(${i})" id="marginSaveBtn-${i}">Guardar</button></td>`;
+            tr.innerHTML = html; tbody.appendChild(tr);
         });
-        checkMarginUnsavedChanges();
     }
-
-    function toggleAllMargin(source) {
-        const checkboxes = document.querySelectorAll('.margin-row-checkbox');
-        checkboxes.forEach(cb => cb.checked = source.checked);
-    }
-
-    function checkMarginUnsavedChanges() {
-        let hasUnsaved = false;
-        document.querySelectorAll('.margin-invoice-row').forEach(tr => {
-            let rowUnsaved = false;
-            const marginInputs = tr.querySelectorAll('.margin-input');
-            const statusSelect = tr.querySelector('.margin-status-select');
-            const paydateInput = tr.querySelector('.margin-paydate-input');
-            
-            marginInputs.forEach(i => { if(i.value !== i.getAttribute('data-original')) rowUnsaved = true; });
-            if (statusSelect && statusSelect.value !== statusSelect.getAttribute('data-original')) rowUnsaved = true;
-            if (paydateInput && paydateInput.value !== paydateInput.getAttribute('data-original')) rowUnsaved = true;
-            
-            if (rowUnsaved) {
-                tr.classList.add('unsaved');
-                hasUnsaved = true;
-            } else {
-                tr.classList.remove('unsaved');
-            }
-        });
-        
-        const saveBtn = document.getElementById('saveAllMarginsBtn');
-        if (saveBtn) {
-            if (hasUnsaved) saveBtn.classList.add('pulse-red');
-            else saveBtn.classList.remove('pulse-red');
-        }
-    }
-
-    async function saveAllMargins() {
-        const checkboxes = document.querySelectorAll('.margin-row-checkbox:checked');
-        const rowsToSave = new Set();
-        
-        // Include unsaved changes implicitly
-        document.querySelectorAll('.margin-invoice-row.unsaved .margin-row-checkbox').forEach(cb => rowsToSave.add(cb.value));
-        checkboxes.forEach(cb => rowsToSave.add(cb.value));
 
-        if (rowsToSave.size === 0) {
-            showToast('Selecciona filas con el checkbox o asegúrate de haber editado algún campo para guardar.', 'warning');
-            return;
-        }
-
-        const btn = document.getElementById('saveAllMarginsBtn');
-        btn.disabled = true;
-        const oT = btn.innerHTML;
-        btn.innerHTML = '⏳ Guardando...';
-
-        const payload = [];
-
-        Array.from(rowsToSave).forEach(idx => {
-            const index = parseInt(idx);
-            const record = marginData[index];
-            
-            const marginUpdates = {};
-            const inputs = document.querySelectorAll(`input.margin-input[data-row="${index}"]`);
-            inputs.forEach(input => {
-                marginUpdates[input.dataset.key] = input.value;
-            });
-
-            const statusNode = document.querySelector(`.margin-status-select[data-row="${index}"]`);
-            const paydateNode = document.querySelector(`.margin-paydate-input[data-row="${index}"]`);
-
-            let sheetDate = paydateNode ? paydateNode.value : '';
-            if (sheetDate && sheetDate.includes('-')) {
-                const parts = sheetDate.split('-');
-                if (parts.length === 3) sheetDate = `${parts[2]}/${parts[1]}/${parts[0]}`;
-            }
-
-            payload.push({
-                row_index: record._sheet_row_index,
-                margin_updates: marginUpdates,
-                status: statusNode ? statusNode.value : undefined,
-                payment_date: sheetDate
-            });
-        });
-
+    async function saveMarginRow(i) {
+        const rec = marginData[i], btn = document.getElementById(`marginSaveBtn-${i}`);
+        btn.disabled = true; btn.textContent = '…';
+        const updates = {};
+        document.querySelectorAll(`input.margin-input[data-row="${i}"]`).forEach(inp => { updates[inp.dataset.key] = inp.value; });
         try {
-            const response = await fetch('/api/update_margin', {
-                method: 'POST',
-                headers: { 'Content-Type': 'application/json' },
-                body: JSON.stringify(payload)
-            });
-            const result = await response.json();
-
-            if (response.ok && result.success) {
-                showToast(`¡${rowsToSave.size} fila(s) actualizada(s) con éxito!`);
-                // update originals
-                Array.from(rowsToSave).forEach(idx => {
-                    const index = parseInt(idx);
-                    document.querySelectorAll(`input.margin-input[data-row="${index}"]`).forEach(i => i.setAttribute('data-original', i.value));
-                    const s = document.querySelector(`.margin-status-select[data-row="${index}"]`);
-                    if(s) s.setAttribute('data-original', s.value);
-                    const p = document.querySelector(`.margin-paydate-input[data-row="${index}"]`);
-                    if(p) p.setAttribute('data-original', p.value);
-                });
-                document.querySelectorAll('.margin-row-checkbox:checked').forEach(cb => cb.checked = false);
-                checkMarginUnsavedChanges();
-                // Refresh iframe
-                const sheetsIframe = document.querySelector('#sheetsView iframe');
-                if (sheetsIframe) sheetsIframe.src = sheetsIframe.src;
-            } else {
-                showToast('Error al actualizar: ' + result.message, 'error');
-            }
-        } catch (error) {
-            showToast('Error de conexión con el servidor', 'error');
-        } finally {
-            btn.disabled = false;
-            btn.innerHTML = oT;
-        }
+            const res = await fetch('/api/update_margin', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ row_index: rec._sheet_row_index, margin_updates: updates })});
+            const r = await res.json();
+            if (res.ok && r.success) { showToast(`${rec._invoice_id} actualizado`); btn.textContent = '✓'; setTimeout(() => { btn.textContent = 'Guardar'; btn.disabled = false; }, 1500); }
+            else { showToast('Error: ' + r.message, 'error'); btn.textContent = 'Guardar'; btn.disabled = false; }
+        } catch (e) { showToast('Error de conexión', 'error'); btn.textContent = 'Guardar'; btn.disabled = false; }
     }
 
-    // ===== New Invoice Modal =====
-    function openNewInvoiceModal() {
-        document.getElementById('newInvoiceModal').classList.add('active');
-    }
-
+    // ===== Modal =====
+    function openNewInvoiceModal() { document.getElementById('newInvoiceModal').classList.add('active'); }
     function closeNewInvoiceModal() {
         document.getElementById('newInvoiceModal').classList.remove('active');
-        // Clear fields
-        ['newClientName','newPosition','newCandidateName','newStartDate','newFixSalary','newInvoiceDate','newFeePercent','newInvoiceAmount'].forEach(id => {
-            document.getElementById(id).value = '';
-        });
+        ['newClientName','newPosition','newCandidateName','newStartDate','newFixSalary','newInvoiceDate','newFeePercent','newInvoiceAmount'].forEach(id => document.getElementById(id).value = '');
     }
 
     async function submitNewInvoice() {
-        const clientName = document.getElementById('newClientName').value.trim();
-        const candidateName = document.getElementById('newCandidateName').value.trim();
-        
-        if (!clientName || !candidateName) {
-            alert('El Nombre del Cliente y el Nombre del Candidato son obligatorios.');
-            return;
-        }
-        
-        const btn = document.getElementById('createInvoiceBtn');
-        btn.disabled = true;
-        btn.textContent = 'Creando...';
-        
-        // Convert dates from YYYY-MM-DD to DD/MM/YYYY
-        function formatDate(val) {
-            if (!val) return '';
-            const parts = val.split('-');
-            if (parts.length === 3) return `${parts[2]}/${parts[1]}/${parts[0]}`;
-            return val;
-        }
-        
-        const payload = {
-            client_name: clientName,
-            position: document.getElementById('newPosition').value.trim(),
-            candidate_name: candidateName,
-            start_date: formatDate(document.getElementById('newStartDate').value),
-            fix_salary: document.getElementById('newFixSalary').value,
-            invoice_date: formatDate(document.getElementById('newInvoiceDate').value),
-            fee_percent: document.getElementById('newFeePercent').value,
-            invoice_amount: document.getElementById('newInvoiceAmount').value,
-            status: 'Pending'
-        };
-        
+        const cn = document.getElementById('newClientName').value.trim();
+        const cand = document.getElementById('newCandidateName').value.trim();
+        if (!cn || !cand) { alert('El nombre del cliente y del candidato son obligatorios.'); return; }
+        const btn = document.getElementById('createInvoiceBtn'); btn.disabled = true; btn.textContent = 'Creando…';
+        const fmt = v => { if (!v) return ''; const p = v.split('-'); return p.length === 3 ? `${p[2]}/${p[1]}/${p[0]}` : v; };
         try {
-            const response = await fetch('/api/create_invoice', {
-                method: 'POST',
-                headers: { 'Content-Type': 'application/json' },
-                body: JSON.stringify(payload)
-            });
-            const result = await response.json();
-            
-            if (response.ok && result.success) {
-                showToast(`¡Factura ${result.invoice_id} creada!`);
-                closeNewInvoiceModal();
-                // Refresh both pages
-                fetchData();
-                fetchMarginData();
-                // Refresh Google Sheets iframe
-                const sheetsIframe = document.querySelector('#sheetsView iframe');
-                if (sheetsIframe) sheetsIframe.src = sheetsIframe.src;
-            } else {
-                showToast('Error: ' + result.message, 'error');
-            }
-        } catch (error) {
-            showToast('Error de conexión con el servidor', 'error');
-        } finally {
-            btn.disabled = false;
-            btn.textContent = 'Crear Factura';
-        }
+            const res = await fetch('/api/create_invoice', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({
+                client_name: cn, position: document.getElementById('newPosition').value.trim(), candidate_name: cand,
+                start_date: fmt(document.getElementById('newStartDate').value), fix_salary: document.getElementById('newFixSalary').value,
+                invoice_date: fmt(document.getElementById('newInvoiceDate').value), fee_percent: document.getElementById('newFeePercent').value,
+                invoice_amount: document.getElementById('newInvoiceAmount').value, status: 'Pending'
+            })});
+            const r = await res.json();
+            if (res.ok && r.success) { showToast(`Factura ${r.invoice_id} creada`); closeNewInvoiceModal(); fetchData(); fetchMarginData(); const sf = document.querySelector('#sheetsView iframe'); if (sf) sf.src = sf.src; }
+            else showToast('Error: ' + r.message, 'error');
+        } catch (e) { showToast('Error de conexión', 'error'); }
+        finally { btn.disabled = false; btn.textContent = 'Crear Factura'; }
     }
 
-    // ===== Co-pilot Logic =====
+    // ===== Chat =====
     let chatHistoryArr = [];
-
     async function sendMessage() {
-        const inputStr = document.getElementById('chatInput');
-        const userText = inputStr.value.trim();
-        if (!userText) return;
-        
-        appendMessage(userText, 'user');
-        inputStr.value = '';
-        
-        const sendBtn = document.getElementById('sendBtn');
-        sendBtn.disabled = true;
-        sendBtn.innerText = 'Pensando...';
-        
+        const inp = document.getElementById('chatInput'); const text = inp.value.trim(); if (!text) return;
+        appendMessage(text, 'user'); inp.value = '';
+        const btn = document.getElementById('sendBtn'); btn.disabled = true; btn.innerText = 'Pensando…';
         try {
-            const selectedModel = localStorage.getItem('biloop_gemini_model') || 'gemini-2.5-flash';
-            const response = await fetch('/api/copilot', {
-                method: 'POST',
-                headers: { 'Content-Type': 'application/json' },
-                body: JSON.stringify({ message: userText, history: chatHistoryArr, model: selectedModel })
-            });
-            const data = await response.json();
-            
-            if (response.ok && data.success) {
-                appendMessage(data.reply, 'model');
-                chatHistoryArr.push({ role: 'user', content: userText });
-                chatHistoryArr.push({ role: 'model', content: data.reply });
-            } else {
-                appendMessage('Error: ' + data.message, 'error');
-            }
-        } catch (error) {
-            appendMessage('No se pudo conectar con el servidor de IA.', 'error');
-        } finally {
-            sendBtn.disabled = false;
-            sendBtn.innerText = 'Preguntar ✨';
-        }
+            const res = await fetch('/api/copilot', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ message: text, history: chatHistoryArr })});
+            const d = await res.json();
+            if (res.ok && d.success) { appendMessage(d.reply, 'model'); chatHistoryArr.push({ role: 'user', content: text }, { role: 'model', content: d.reply }); }
+            else appendMessage('Error: ' + d.message, 'error');
+        } catch (e) { appendMessage('No se pudo conectar con el servidor de IA.', 'error'); }
+        finally { btn.disabled = false; btn.innerHTML = '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg> Enviar'; }
     }
 
     function appendMessage(text, role) {
-        const chatHistory = document.getElementById('chatHistory');
-        const msgDiv = document.createElement('div');
-        msgDiv.style.padding = '16px';
-        msgDiv.style.borderRadius = '8px';
-        msgDiv.style.maxWidth = '80%';
-        msgDiv.style.whiteSpace = 'pre-wrap';
-        msgDiv.style.lineHeight = '1.5';
-        
+        const chat = document.getElementById('chatHistory');
         if (role === 'user') {
-            msgDiv.style.background = 'rgba(255, 255, 255, 0.05)';
-            msgDiv.style.color = 'var(--text-main)';
-            msgDiv.style.alignSelf = 'flex-end';
-            msgDiv.style.border = '1px solid var(--border-color)';
+            const div = document.createElement('div');
+            div.className = 'chat-msg user';
+            div.innerHTML = text.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>');
+            chat.appendChild(div);
         } else if (role === 'error') {
-            msgDiv.style.background = 'rgba(239, 68, 68, 0.1)';
-            msgDiv.style.color = 'var(--error)';
-            msgDiv.style.alignSelf = 'center';
-            msgDiv.style.border = '1px solid rgba(239, 68, 68, 0.3)';
+            const div = document.createElement('div');
+            div.className = 'chat-msg error';
+            div.textContent = text;
+            chat.appendChild(div);
         } else {
-            msgDiv.style.background = 'rgba(79, 70, 229, 0.1)';
-            msgDiv.style.color = 'var(--text-main)';
-            msgDiv.style.alignSelf = 'flex-start';
+            const wrapper = document.createElement('div');
+            wrapper.className = 'chat-msg model';
+            wrapper.innerHTML = `
+                <div class="ai-avatar">
+                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>
+                </div>
+                <div class="msg-content">${text.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')}</div>
+            `;
+            chat.appendChild(wrapper);
         }
-        
-        let formattedText = text.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>');
-        msgDiv.innerHTML = formattedText;
-        
-        chatHistory.appendChild(msgDiv);
-        chatHistory.scrollTop = chatHistory.scrollHeight;
+        chat.scrollTop = chat.scrollHeight;
     }
 
-    // Fill chatInput from a chip click, trim emoji prefix
-    function fillChip(text) {
-        // Strip leading emoji plus space (e.g. "💰 " → "")
-        const cleaned = text.replace(/^[\p{Emoji}\s]+/u, '').trim();
-        const input = document.getElementById('chatInput');
-        input.value = cleaned;
-        input.focus();
-    }
+    function fillChip(t) { const inp = document.getElementById('chatInput'); inp.value = t.trim(); inp.focus(); }
+    function handleChatKeyPress(e) { if (e.key === 'Enter') sendMessage(); }
 
-    function handleChatKeyPress(event) {
-        if (event.key === 'Enter') {
-            sendMessage();
-        }
-    }
-
-    // ===== Configuration =====
-    function loadConfig() {
-        const savedModel = localStorage.getItem('biloop_gemini_model') || 'gemini-2.5-flash';
-        const select = document.getElementById('geminiModelSelect');
-        if (select) {
-            select.value = savedModel;
-        }
-    }
-
-    function saveGeminiModel() {
-        const select = document.getElementById('geminiModelSelect');
-        if (select) {
-            localStorage.setItem('biloop_gemini_model', select.value);
-            showToast('Modelo de Gemini actualizado correctamente');
-        }
-    }
-
-    // Init fetch
-    window.onload = () => {
-        fetchData();
-        fetchMarginData();
-        loadConfig();
-    };
+    // ===== Init =====
+    document.addEventListener("DOMContentLoaded", () => { fetchData(); fetchMarginData(); });
 </script>
-
 </body>
 </html>
